version: '3.8'

services:
  wifi-controller:
    build: .
    container_name: wificontroller
    network_mode: host
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_ADMIN
    privileged: true
    volumes:
      - .:/app
